# Welle 9 — Cut-Liste `linux-desktop-chat-infra`

Host-Verzeichnisse **`app/debug/`**, **`app/metrics/`**, **`app/tools/`** entfernt; Quelle: **`linux-desktop-chat-infra/src/app/{debug,metrics,tools}/`**.

Importpfade unverändert: `app.debug.*`, `app.metrics.*`, `app.tools.*`.
