Hier ist eine **typische** Assistentenantwort.

Kurze Liste:

- Punkt A
- Punkt B mit `code`

Installieren:

```bash
uv pip install -e .
```

Struktur im Projekt:

```
linux-desktop-chat/
├── app/
│   └── gui/
└── tests/
```

Falls etwas schiefgeht:

```
Traceback (most recent call last):
  File "main.py", line 10, in <module>
    run()
RuntimeError: not configured
```

Passt das so für dich?
