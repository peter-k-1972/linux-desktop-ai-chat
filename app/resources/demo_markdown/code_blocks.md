# Codeblöcke

Inline: nutze `pip install requests` im Terminal.

Fenced Block:

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"

if __name__ == "__main__":
    print(greet("World"))
```

Ohne Sprachhinweis:

```
plain text
  indented
```
