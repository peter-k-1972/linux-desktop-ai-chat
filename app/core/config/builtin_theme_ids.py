"""
Built-in theme IDs known to core (no GUI dependency).

Must stay in sync with built-in themes in ``app.gui.themes.registry.BUILTIN_THEME_IDS``.
"""

# light_default, dark_default, workbench — siehe ThemeRegistry
BUILTIN_THEME_IDS = frozenset({"light_default", "dark_default", "workbench"})
