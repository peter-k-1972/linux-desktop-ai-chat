import QtQuick

/**
 * Registrier-Metadaten für aktive Theme-Variante (Erweiterungspunkt für weitere Manifeste).
 */
QtObject {
    readonly property string activeVariantId: "library_dark"
    readonly property string displayLabel: "Bibliothek · dunkel"
}
