# Vollständiges Entwickler-/CI-Set: PEP-621-Basis (core) + optional-dependencies rag + dev.
# Entspricht der früheren monolithischen Liste; Quelle der Wahrheit für Namen/Gruppen: pyproject.toml
# + app.features.dependency_packaging (Drift-Check: validate_pep621_pyproject_alignment).
#
# Von Repo-Root:
#   pip install -r requirements.txt
# oder direkt:
#   pip install -e ".[rag,dev]"
-e .[rag,dev]
